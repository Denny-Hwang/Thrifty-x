# Relative to this file: `cmake --install --prefix X` must point at X,
# not at the CMAKE_INSTALL_PREFIX the build was configured with.
prefix=${pcfiledir}/../..
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: fastdet
Description: Fast beacon detection using RTL-SDR
Version: @VERSION_INFO_MAJOR_VERSION@.@VERSION_INFO_MINOR_VERSION@
Cflags: -I${includedir}/ @FASTDET_PC_CFLAGS@
# Public: AlignedArray in fastcard_wrappers.h calls volk_malloc and
# volk_free inline, so a program built on the headers links volk itself.
Requires: volk
Requires.private: fastcapture
Libs: -L${libdir} -lfastdet
Libs.private: @FASTDET_PC_LIBS@
